# Task 4: Decorator with Arguments
# Problem:
# Create a decorator multiply_decorator that takes
# a parameter factor and multiplies the return value
# of a function by that factor.
# Apply multiply_decorator to a function get_price()
# that returns a price (e.g., 50).
# Call the decorated function with a factor of 2 and
# print the result.
# Example Output:

# 100
